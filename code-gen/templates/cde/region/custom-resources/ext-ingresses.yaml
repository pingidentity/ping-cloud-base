# # NOTES:
# # - This custom patch is not applied to the cluster by default.
# #
# # - Adding this patch will expose admin services listed below
# #   to the public.
# #
# # - Make sure WAF is enabled for all the public endpoints before
# #   applying this patch.
# #

# # This defines the external ingress for the pingdelegator service

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: pingdelegator-ext-ingress
#   namespace: ${PING_CLOUD_NAMESPACE}
#   annotations:
#     nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
#     nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
#     self-service.metadata.pingidentity.com/name: "Delegated Admin Console"
#     self-service.metadata.pingidentity.com/category: "Product Administration"
#     self-service.metadata.pingidentity.com/displayURL: "https://ext-pingdelegator.${DNS_ZONE}/delegator"
#   labels:
#     self-service.urls/managed: "true"
# spec:
#   ingressClassName: nginx-public
#   tls:
#   - hosts:
#     - ext-pingdelegator.${DNS_ZONE}
#   rules:
#   - host: ext-pingdelegator.${DNS_ZONE}
#     http:
#       paths:
#       - pathType: Prefix
#         path: /
#         backend:
#           service:
#             name: pingdelegator
#             port:
#               number: 1443