#
# This defines the healthcheck-httpbin deployment.
#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcheck-httpbin
  labels:
    role: healthcheck-httpbin
spec:
  selector:
    matchLabels:
      role: healthcheck-httpbin
  template:
    metadata:
      name: healthcheck-httpbin
      labels:
        role: healthcheck-httpbin
    spec:
      containers:
      - name: healthcheck-httpbin
        image: public.ecr.aws/r2h3l6e4/pingcloud-clustertools/go-httpbin:2.22.1
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 65534
          runAsNonRoot: true
          runAsGroup: 65534
          privileged: false
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
        volumeMounts:
        - name: tmp-dir
          mountPath: /tmp
        resources:
          limits:
            memory: "60Mi"
            cpu: "200m"
          requests:
            memory: "10Mi"
            cpu: "100m"
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
          successThreshold: 1
          timeoutSeconds: 3
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
          successThreshold: 1
          timeoutSeconds: 3
        ports:
        - containerPort: 8080
      volumes:
      - name: tmp-dir
        emptyDir: {}
      securityContext:
        runAsNonRoot: true
---
#
# This service exposes the healthcheck-httpbin port to the cluster.
#
apiVersion: v1
kind: Service
metadata:
  name: healthcheck-httpbin
  labels:
    role: healthcheck-httpbin
spec:
  externalName: healthcheck-httpbin.health.svc.cluster.local
  ports:
  - port: 80
    targetPort: 8080
  selector:
    role: healthcheck-httpbin
