kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: external-dns

resources:
- namespace.yaml
- serviceaccount.yaml
- clusterrole.yaml
- clusterrolebinding.yaml
- deployment.yaml

images:
- name: registry.k8s.io/external-dns/external-dns
  newName: public.ecr.aws/r2h3l6e4/pingcloud-clustertools/external-dns/external-dns
  newTag: v0.20.0

patches:
- target:
    kind: Deployment
    name: external-dns
  patch: |
    - op: add
      path: /metadata/labels
      value: 
          app: external-dns
    - op: add 
      path: /spec/template/spec/securityContext
      value:
          fsGroup: 65534 # For ExternalDNS to be able to read Kubernetes and AWS token files
          runAsNonRoot: true
    - op: add
      path: /spec/template/spec/containers/0/resources
      value:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 300m
            memory: 256Mi
    - op: replace
      path: /spec/template/spec/containers/0/args
      value:
          - --source=service
          - --source=ingress
          - --provider=aws
          - --policy=sync # create, update and delete DNS entries so that the provider’s hosted zone exactly mirrors the resources visible in the cluster.
          - --aws-zone-type=public # only look at public hosted zones (valid values are public, private or no value for both)
          - --registry=txt
          - --publish-internal-services
          - --log-level=error
          - --exclude-record-types=AAAA
    - op: add
      path: /spec/template/spec/containers/0/securityContext
      value:
          allowPrivilegeEscalation: false
          privileged: false
          readOnlyRootFilesystem: true
          runAsUser: 65534
          runAsGroup: 65534