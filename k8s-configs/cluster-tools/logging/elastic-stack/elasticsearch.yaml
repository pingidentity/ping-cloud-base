---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: es-cluster
spec:
  serviceName: elasticsearch
  replicas: 3
  selector:
    matchLabels:
      app: elasticsearch
  template:
    metadata:
      labels:
        app: elasticsearch
    spec:
      affinity:
        podAntiAffinity:
          # Add a hard requirement for each elastic-search pod to be deployed to a different node
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - elasticsearch
            topologyKey: "kubernetes.io/hostname"
          # Add a soft requirement for each elastic-search pod to be deployed to a different AZ
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - elasticsearch
              topologyKey: "failure-domain.beta.kubernetes.io/zone"

      initContainers:

      - name: fix-permissions
        image: busybox
        command: [ "sh", "-c", "chown -R 1000:1000 /usr/share/elasticsearch/data" ]

        securityContext:
          privileged: true

        volumeMounts:
        - name: data
          mountPath: /usr/share/elasticsearch/data


      - name: increase-vm-max-map
        image: busybox
        command: [ "sysctl", "-w", "vm.max_map_count=262144" ]

        securityContext:
          privileged: true


      - name: increase-fd-ulimit
        image: busybox
        command: [ "sh", "-c", "ulimit -n 65536" ]

        securityContext:
          privileged: true

      
      - name: set-bootstrap-password
        image: gcr.io/ping-identity/ping-cloud-enrichment-bootstrap/ping-cloud-enrichment-bootstrap:7.8.0.4
        imagePullPolicy: Always
        workingDir: /scripts
        command: ["sh", '$(CONTAINER_NAME).sh']

        securityContext:
          runAsUser: 1000
          runAsGroup: 1000

        env:
        - name: CONTAINER_NAME
          value: "set-bootstrap-password"
        - name: ES_PATH_CONF
          value: "/usr/share/elasticsearch/data/config"
        - name: CERTS_DIR
          value: "/enrichment-shared-volume/certs"
        - name: ELASTIC_BOOTSTRAP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: elk-secrets
              key: ELASTIC_BOOTSTRAP_PASSWORD_B64

        volumeMounts:
        - name: data
          mountPath: /usr/share/elasticsearch/data
          readOnly: false


      - name: wait-for-available-certs
        image: gcr.io/ping-identity/ping-cloud-enrichment-bootstrap/ping-cloud-enrichment-bootstrap:7.8.0.4
        imagePullPolicy: Always
        command: ["sh", '$(CONTAINER_NAME).sh']

        env:
        - name: CONTAINER_NAME
          value: "create-certs"
        - name: CERTS_DIR
          value: "/enrichment-shared-volume/certs"

        volumeMounts:
        - name: enrichment-shared-volume
          mountPath: /enrichment-shared-volume


      containers:

      - name: elasticsearch
        image: docker.elastic.co/elasticsearch/elasticsearch:7.8.0

        securityContext:
          runAsUser: 1000
          runAsGroup: 1000

        resources:
          limits:
            memory: 2Gi
            cpu: 1000m
          requests:
            memory: 512Mi
            cpu: 100m

        ports:
        - containerPort: 9200
          name: rest
          protocol: TCP
        - containerPort: 9300
          name: inter-node
          protocol: TCP

        env:
        - name: CONTAINER_NAME
          value: "elasticsearch"
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        # X-Pack security enabled
        - name: XPACK_SECURITY_ENABLED
          value: "true"
        # List of ES nodes to be searched for making a cluster
        - name: DISCOVERY_SEED_HOSTS
          value: "es-cluster-0.elasticsearch"
        - name: CLUSTER_INITIAL_MASTER_NODES
          value: "es-cluster-0"
        - name: ES_PATH_CONF
          value: "/usr/share/elasticsearch/data/config"
        - name: ES_JAVA_OPTS
          value: "-Xms512m -Xmx512m"
        # ES SSL settings
        - name: XPACK_SECURITY_HTTP_SSL_ENABLED
          value: "true"
        - name: XPACK_SECURITY_HTTP_SSL_VERIFICATION_MODE
          value: "certificate"
        - name: XPACK_SECURITY_HTTP_SSL_CLIENT_AUTHENTICATION
          value: "optional"
        - name: XPACK_SECURITY_HTTP_SSL_KEY
          value: "certs/elasticsearch-http/elasticsearch-http.key"
        - name: XPACK_SECURITY_HTTP_SSL_CERTIFICATE
          value: "certs/elasticsearch-http/elasticsearch-http.crt"
        - name: XPACK_SECURITY_HTTP_SSL_CERTIFICATE_AUTHORITIES
          value: "certs/ca/ca.crt" 
        - name: XPACK_SECURITY_TRANSPORT_SSL_ENABLED
          value: "true"
        - name: XPACK_SECURITY_TRANSPORT_SSL_VERIFICATION_MODE
          value: "certificate"
        - name: XPACK_SECURITY_TRANSPORT_SSL_KEY
          value: "certs/elasticsearch/elasticsearch.key"
        - name: XPACK_SECURITY_TRANSPORT_SSL_CERTIFICATE
          value: "certs/elasticsearch/elasticsearch.crt"
        - name: XPACK_SECURITY_TRANSPORT_SSL_CERTIFICATE_AUTHORITIES
          value: "certs/ca/ca.crt"


        volumeMounts:
        - name: data
          mountPath: /usr/share/elasticsearch/data
          readOnly: false
        - name: enrichment-shared-volume
          mountPath: /enrichment-shared-volume
          readOnly: false
        - name: enrichment-elasticsearch-yaml-config
          mountPath: /usr/share/elasticsearch/data/config/elasticsearch.yml
          subPath: elasticsearch.yml

      volumes:
        - name: enrichment-shared-volume
          nfs:
            server: ${EFS_FILESYSTEM_ID}.efs.${REGION}.amazonaws.com
            path: /
        - name: enrichment-elasticsearch-yaml-config
          configMap:
            name: enrichment-elasticsearch-yaml-config
            
  volumeClaimTemplates:
  - metadata:
      name: data
      labels:
        app: elasticsearch
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: elasticsearch-gp2
      resources:
        requests:
          storage: 40Gi

---
kind: Service
apiVersion: v1
metadata:
  name: elasticsearch
  labels:
    app: elasticsearch
spec:
  selector:
    app: elasticsearch
  clusterIP: None
  ports:
    - port: 9200
      name: rest
    - port: 9300
      name: inter-node

---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: elasticsearch-io1
  labels:
    app: elasticsearch
provisioner: kubernetes.io/aws-ebs
parameters:
  type: io1
  fsType: ext4
  iopsPerGB: "50"
mountOptions:
  - discard

---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: elasticsearch-gp2
  labels:
    app: elasticsearch
provisioner: kubernetes.io/aws-ebs
parameters:
  type: gp2
  fsType: ext4
mountOptions:
  - discard
