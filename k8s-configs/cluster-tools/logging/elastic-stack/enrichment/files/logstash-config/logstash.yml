http.host: "0.0.0.0"
node.name: "${NODE_NAME}"
config.reload.automatic: "${CONFIG_RELOAD_AUTOMATIC}"
config.reload.interval: "${CONFIG_RELOAD_INTERVAL}"
log.format: "${LOG_FORMAT}"
log.level: "${LOG_LEVEL}"