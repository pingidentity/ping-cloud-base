apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: logs-ingress
  annotations:
    selfservice.metadata.pingidentity.com/name: "OpenSearch Dashboards"
    selfservice.metadata.pingidentity.com/description: "Native OpenSearch console for visualizing and analyzing logs"
    selfservice.metadata.pingidentity.com/category: "Platform Administration"
    selfservice.metadata.pingidentity.com/displayURL: "https://logs.ping-demo.com/"
  labels:
    selfservice: urls
spec:
  ingressClassName: nginx-public
  rules:
  - host: logs.ping-demo.com
    http:
      paths:
      - backend:
          service:
            name: pingaccess-was
            port:
              number: 443
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - logs.ping-demo.com
