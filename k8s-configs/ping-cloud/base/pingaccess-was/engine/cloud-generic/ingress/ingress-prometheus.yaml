apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus-ingress
  annotations:
    self-service.metadata.pingidentity.com/name: "Prometheus Console"
    self-service.metadata.pingidentity.com/description: "Native Prometheus expression browser for querying raw metrics"
    self-service.metadata.pingidentity.com/category: "Platform Administration"
    self-service.metadata.pingidentity.com/displayURL: "https://prometheus.ping-demo.com/"
  labels:
    self-service.urls/managed: "true"
spec:
  ingressClassName: nginx-public
  rules:
  - host: prometheus.ping-demo.com
    http:
      paths:
      - backend:
          service:
            name: pingaccess-was
            port:
              number: 443
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - prometheus.ping-demo.com
