ORCHESTRATION_TYPE=kubernetes
VERBOSE=false

# TODO: Change me to https://github.com/pingidentity/ping-cloud-base.git before v1.13 is released.
SERVER_PROFILE_URL=https://git-codecommit.us-west-2.amazonaws.com/v1/repos/ping-cloud-base
SERVER_PROFILE_PATH=profiles/aws/pingdatasync
SERVER_PROFILE_BRANCH=v1.13-release-branch

REGION=us-west-2

PING_IDENTITY_ACCEPT_EULA=YES

LDAP_PORT=1389
LDAPS_PORT=1636
HTTPS_PORT=1443

# Location of file with the root user password (i.e. cn=directory manager).
# Defaults to /SECRETS_DIR/root-user-password
ROOT_USER_PASSWORD_FILE=/usr/local/secrets/root-user-password

# FIXME: Look into the environments below this comment to see if we really need.
# PDO-2995 will address this issue.

# Failover administrative user
ADMIN_USER_NAME=admin

# Location of file with the admin password, used as the password replication admin
# Defaults to /SECRETS_DIR/admin-user-password
ADMIN_USER_PASSWORD_FILE=/usr/local/secrets/admin-user-password