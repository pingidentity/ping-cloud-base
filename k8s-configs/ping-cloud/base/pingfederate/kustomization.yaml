kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

resources:
- deployment.yaml
- service.yaml
- ingress.yaml

commonLabels:
  role: pingfederate

configMapGenerator:
- name: pingfederate-environment-variables
  env: env_vars

secretGenerator:
- name: pingfederate-secret-variables
  env: secret_vars