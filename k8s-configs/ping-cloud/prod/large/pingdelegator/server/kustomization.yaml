kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: ping-cloud


resources:
- ../../../../base/pingdelegator/server/aws

labels:
- includeSelectors: true
  pairs:
    app: ping-cloud
patches:
- path: limits.yaml
