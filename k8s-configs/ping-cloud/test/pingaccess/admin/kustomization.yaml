kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: ping-cloud


resources:
- ../../../base/pingaccess/admin/aws

labels:
- includeSelectors: true
  pairs:
    app: ping-cloud
patches:
- path: admin-init-env.yaml
