kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: ping-cloud


resources:
- ../../../base/pingdirectory/server/aws

labels:
- includeSelectors: true
  pairs:
    app: ping-cloud
    role: pingdirectory
patches:
- path: wait-for-service.yaml
