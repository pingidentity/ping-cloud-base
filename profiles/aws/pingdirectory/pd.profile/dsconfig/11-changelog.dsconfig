#
# Enabling the changelog allows the use of Sync
#
dsconfig set-backend-prop \
    --backend-name changelog \
    --set enabled:true \
    --set "changelog-maximum-age:${P1AS_PD_SERVER_CHANGELOG_SYNC_TIME}"