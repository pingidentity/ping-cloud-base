apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pingaccess
spec:
  template:
    spec:
      initContainers:
      - name: pingaccess-init
        env:
        # Overwrite default value to wait for admin as admin do not exist in sub cluster.
        - name: WAIT_FOR_SERVICES
          value: ''