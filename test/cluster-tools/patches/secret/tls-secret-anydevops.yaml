apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: tls-secret-anydevops
  namespace: chronograf
  labels:
    role: chronograf
data:
  tls.crt: ${TLS_CRT_BASE64}
  tls.key: ${TLS_KEY_BASE64}